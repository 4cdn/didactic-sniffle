      <div class="message" id="${articlehash}">
        <a href="img/${imagelink}" class="image_link" target="_blank" title="${imagename}"><img src="thumbs/${thumblink}" class="image" /></a>
        <a name="${articlehash}"></a><span class="topicline"><b>${subject}</b> ${author} <span class=published>${sent} UTC</span>  <a href="thread-${parenthash}.html#${articlehash}" onclick="return highlight('${articlehash}');">№</a> <a href="#i${articlehash}" onmouseover="get_quote();" onclick="return quickreply('${articlehash}', '${parenthash_full}');">${articlehash}</a></span>
        ${signed}          <div class="stillevil">
          <input type="checkbox" value="${articlehash_full}" title="purge" class="purge_msg" name="purge" id="purge${articlehash}">
          <label for="purge${articlehash}">delete post</label>
          <input type="checkbox" value="${articlehash_full}" title="delete attachment" name="delete_a" id="del_a${articlehash}">
          <label for="del_a${articlehash}">delete attachment</label>
          <input type="checkbox" value="${articlehash_full}" title="Delete all i2p-user's posts" class="purge_msg" name="purge_desthash" id="purge_desthash${articlehash}">
          <label for="purge_desthash${articlehash}">Delete all i2p-user's posts</label>
        </div><br />
        <span class="frontend">[ ${frontend} ]</span>
        <br /><br />
        <span class="message_span">${message}
        </span>
        <br style="clear: both;"/>
      </div>
